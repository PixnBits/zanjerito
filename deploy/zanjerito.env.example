# Copied once to /opt/zanjerito/zanjerito.env (install does not overwrite).
# LAN trust only (D7). Bind this Pi LAN address only, e.g.:
LISTEN=192.168.1.8:8080
# fake | lockout until gpiocdev is wired
DRIVER=fake
